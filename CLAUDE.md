# Important 
- The name of the application is "Pokaliz Laire" 
- Created by Frimpong Opoku Agyemang 
- Do not cosign when you push to git 
- The plan is to build all of this without reaching the claude Pro limits. And so make yourself a MEMORY and REGISTRY, where you periodically summarize relevant context. For the REGISTRY, use it as a way to map components to their functionality and anything usefull that will help you immediately find what is where. 
- Always read the registry before you go and re-read any files to find context 
- When you require anything of me, be super clear on the exact task and how exactly I should get you what you want. 
- Always track version number. Track version number in a different JSON from the `package.json` committing any set of changes. Use semver: patch (0.x.Y) for bug fixes only, minor (0.X.0) for new features or meaningful UX changes. Then commit the version bump in the same commit as the changes.

- Build on ShadCN components wherever necessary. Use Tailwind. Use NextJS
